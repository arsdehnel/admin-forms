<th data-column-select-priority="{{columnSelectPriority}}" id="{{name}}" data-column-index="{{colIndex}}">
    {{{label}}}
  	{{#if tooltip}}
        <i class="icon icon-tooltip">
		    <span class="tooltip">
		      	{{{tooltip}}}
	    	</span>
    	</i>
  	{{/if}}
</th>